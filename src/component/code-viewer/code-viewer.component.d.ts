import { AfterViewChecked } from '@angular/core';
export declare class DejaCodeViewerComponent implements AfterViewChecked {
    language: string;
    private initialised;
    constructor();
    ngAfterViewChecked(): void;
}
